import { createApp } from 'vue'
import App from './App.vue'

createApp(App).mount('#app')

// import { createApp } from 'vue'
// import App from './App.vue'

// import Child from "./assets/components/childComponent.vue";

// const app = createApp(App);
// app.component("child", Child);

// app.mount("#app");